#include <stdio.h>
#include <math.h>

int dectobin (int);

int main() {
    int dec, bin;
    printf("Input integer and I will return binary equivalent");
    scanf("%d", dec);
    bin = dectobin(dec);
    printf("Number, %d in binary: %d", dec, bin);
}

int dectobin(int x) {
    LET ME COME BACK TO THIS, IDK WHY I FIND IT SO HARD
    printf("%d", r);
}